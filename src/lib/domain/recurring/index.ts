// Framework-agnostic domain logic for recurring transaction templates
// (frequency math, next-run-date computation, generation rules). No Next.js /
// Prisma imports — shared between web and mobile. Filled in later.
export {};
